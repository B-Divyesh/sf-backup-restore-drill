[
  {
    "id": "demo-isolation",
    "claim": "The bundled demo reads shipped sample data and leaves unrelated working-directory files unchanged.",
    "where": "/demo, README",
    "test": "npx playwright test --grep @claim:demo-isolation",
    "sandbox": "temporary working directory containing configuration, backup, and receipt traps"
  },
  {
    "id": "restore-verification",
    "claim": "Selected files pass only when their restored bytes match the saved SHA-256 fingerprint and the file exists.",
    "where": "landing, README",
    "test": "npx playwright test --grep @claim:restore-verification",
    "sandbox": "real CLI with passing, mismatched, and missing restored files"
  },
  {
    "id": "configured-command",
    "claim": "Restore Drill runs the restore command arguments configured by the operator.",
    "where": "landing",
    "test": "npx playwright test --grep @claim:configured-command",
    "sandbox": "argument-recording restore stub receives the temporary restore folder and literal configured argument"
  },
  {
    "id": "temporary-folder-lifecycle",
    "claim": "Restore Drill removes its temporary restore folder before it writes a receipt, including failed checks.",
    "where": "landing, README",
    "test": "npx playwright test --grep @claim:temporary-folder-lifecycle",
    "sandbox": "real CLI pass, restore failure, missing file, hash mismatch, application failure, and timeout cases"
  },
  {
    "id": "application-check",
    "claim": "A failed application check makes the drill fail.",
    "where": "landing, README",
    "test": "npx playwright test --grep @claim:application-check",
    "sandbox": "real CLI with passing and exit-7 application checks"
  },
  {
    "id": "path-safety",
    "claim": "Restore Drill rejects unsafe configuration and restored links outside its temporary restore folder.",
    "where": "landing, README",
    "test": "npx playwright test --grep @claim:path-safety",
    "sandbox": "invalid command configuration plus a real restore command that creates an outside link"
  },
  {
    "id": "receipt-integrity",
    "claim": "Receipts are read-only JSON files linked by SHA-256, and changed receipts are detected.",
    "where": "landing, README",
    "test": "npx playwright test --grep @claim:receipt-integrity",
    "sandbox": "two real CLI receipts followed by a deliberate replacement"
  },
  {
    "id": "receipt-redaction",
    "claim": "Receipts do not copy backup-command output or restored file contents.",
    "where": "README",
    "test": "npx playwright test --grep @claim:receipt-redaction",
    "sandbox": "real restore command emits a unique private value to stdout, stderr, and the restored file"
  },
  {
    "id": "json-and-exit-codes",
    "claim": "Run, status, and receipts provide JSON output and use the documented result codes.",
    "where": "README",
    "test": "npx playwright test --grep @claim:json-and-exit-codes",
    "sandbox": "real CLI current, failed, overdue, never-run, and invalid-configuration cases"
  },
  {
    "id": "browser-privacy",
    "claim": "The website has no forms, analytics, cookies, tracking pixels, or third-party requests.",
    "where": "landing, Privacy, README",
    "test": "npx playwright test --grep @claim:browser-privacy",
    "sandbox": "fresh browser demo with request capture, storage inspection, and reset"
  },
  {
    "id": "offline-reload",
    "claim": "The sample demo reloads offline after the first visit.",
    "where": "landing",
    "test": "npx playwright test --grep @claim:offline-reload",
    "sandbox": "fresh browser context visits the demo, then reloads after network is disabled"
  },
  {
    "id": "license",
    "claim": "Restore Drill is free and MIT licensed.",
    "where": "landing, README, Terms",
    "test": "npx playwright test --grep @claim:license",
    "sandbox": "repository license and Cargo package metadata"
  },
  {
    "id": "build-artifacts",
    "claim": "npm run build creates dist/bin/restore-drill and dist/site/.",
    "where": "README",
    "test": "npx playwright test --grep @claim:build-artifacts",
    "sandbox": "documented build command followed by artifact inspection"
  },
  {
    "id": "response-policy",
    "claim": "The built site includes the documented security-header and cache settings.",
    "where": "README",
    "test": "npx playwright test --grep @claim:response-policy",
    "sandbox": "built staticwebapp.config.json validated against the documented headers and cache rules"
  }
]
